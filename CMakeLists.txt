cmake_minimum_required(VERSION 3.16.0)
project(Calcul VERSION 0.1.0 LANGUAGES C CXX)

include(CTest)
enable_testing()

#add_library(calc_main SimpleCalc.cpp)

add_executable(Calcul main.cpp)

set(CPACK_PROJECT_NAME ${PROJECT_NAME})
set(CPACK_PROJECT_VERSION ${PROJECT_VERSION})
include(CPack)
